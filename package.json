{
  "name": "resume",
  "private": true,
  "version": "1.0.0",
  "description": "Online resume built with reveal.js",
  "license": "MIT",
  "scripts": {
    "postinstall": "node scripts/sync-reveal.mjs",
    "sync": "node scripts/sync-reveal.mjs",
    "build": "node scripts/build-site.mjs",
    "start": "http-server -p 8000 -c-1 .",
    "dev": "npm run start"
  },
  "dependencies": {
    "reveal.js": "^5.0.0"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
